const html = require('choo/html')


module.exports = function personalView (state, emit) {
  // const processData = (e) => {
  //   e.preventDefault();
  //   state.name = e.currentTarget.name.value
  //  }

  return html`
    <div class='column full-height full-width'>
       IM EMPTY PERSONAL SETTINGS
    </div>
  `
}